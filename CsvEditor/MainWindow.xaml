<Window x:Class="CsvEditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CsvEditor"
        mc:Ignorable="d" Title="CsvEditor" Height="400" Width="600">

    <!-- 拡張性を持たせたい -->
    <!-- ツリービュー利用したい -->
    <!-- スタイル適用は後から -->

    <DockPanel Background="Orange">
        <Menu DockPanel.Dock="Top">
            <MenuItem x:Name="mnFile">
                <MenuItem x:Name="mnNewFile"/>
                <MenuItem x:Name="mnSaveFile"/>
                <MenuItem x:Name="mnOverWriteFile"/>
            </MenuItem>
            <MenuItem x:Name="mnSettings"/>
        </Menu>
        <Grid DockPanel.Dock="Top" Background="AliceBlue" Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.5*" />
                <ColumnDefinition Width="7*" />
            </Grid.ColumnDefinitions>

            <UniformGrid Rows="2">
                <Button Click="btnTest_Click">test</Button>
            </UniformGrid>
        </Grid>
    </DockPanel>

</Window>
