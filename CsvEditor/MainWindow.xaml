<Window x:Class="CsvEditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CsvEditor"
        mc:Ignorable="d" Title="CsvEditor" Height="400" Width="600">

    <!-- 拡張性を持たせたい -->
    <!-- ツリービュー利用したい -->
    <!-- スタイル適用は後から -->

    <DockPanel Background="Orange">
        <Menu DockPanel.Dock="Top">
            <MenuItem x:Name="mnFile" Header="ファイル(F)">
                <MenuItem x:Name="mnNewFile" Header="新しいファイル"/>
                <MenuItem x:Name="mnSaveFile" Header="名前を付けて保存"/>
                <MenuItem x:Name="mnOverWriteFile" Header="上書き保存"/>
            </MenuItem>
            <MenuItem x:Name="mnSettings" Header="設定(C)">
            </MenuItem>
        </Menu>
        <Grid DockPanel.Dock="Top" Background="AliceBlue" Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.5*" />
                <ColumnDefinition Width="7*" />
            </Grid.ColumnDefinitions>
        </Grid>
    </DockPanel>

</Window>
